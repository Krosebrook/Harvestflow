# ─────────────────────────────────────────────
# HarvestFlow — Environment Variables Template
# Copy to .env.local and fill in real values.
# NEVER commit .env.local or any file with real secrets.
# ─────────────────────────────────────────────

# ── Server ──────────────────────────────────
PORT=5173
NODE_ENV=development

# ── File Storage ────────────────────────────
# Absolute or relative path to the dropzone root directory.
# In serverless environments (Vercel), use /tmp/dropzone (ephemeral).
DROPZONE_ROOT=./dropzone-data

# ── Basic Authentication (optional) ─────────
# When both are set every route is protected with HTTP Basic Auth.
BASIC_AUTH_USER=
BASIC_AUTH_PASS=

# ── LLM API Keys ────────────────────────────
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
XAI_API_KEY=
PERPLEXITY_API_KEY=

# ── Supabase (Phase 3 – planned) ────────────
SUPABASE_URL=http://localhost:54321
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ── Redis (Phase 3 – planned) ───────────────
REDIS_URL=redis://localhost:6379

# ── Notifications ───────────────────────────
TEAMS_WEBHOOK_URL=

# ── Vercel (set automatically by platform) ──
# VERCEL=1
